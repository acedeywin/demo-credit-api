export enum AccountStatus {
    ACTIVE = 'active',
    DORMANT = 'dormant',
    SUSPENDED = 'suspended',
    CLOSED = 'closed',
}

export enum PaymentType {
    CREDIT = 'credit',
    DEBIT = 'debit',
}
